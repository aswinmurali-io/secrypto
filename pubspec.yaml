name: secrypto
description: Best chat service
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^2.2.2
  google_fonts: ^1.1.1
  google_sign_in: ^4.5.5
  cupertino_icons: ^1.0.0
  flutter_signin_button: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
